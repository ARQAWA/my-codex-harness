interface:
  display_name: "Working Standards"
  short_description: "Apply our full work standards before each task"
  default_prompt: "Use $working-standards before planning or doing this task."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Notebook Cleanup"
  short_description: "Delete the current session's Task Notebook"
  default_prompt: "Use $notebook-cleanup to delete this session's entire Task Notebook."
policy:
  allow_implicit_invocation: false
